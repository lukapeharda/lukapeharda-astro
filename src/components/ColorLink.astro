---
interface Props {
	href: string;
    color: string;
}

const { href, color } = Astro.props;
---
<a href={ href } class={ `border-b border-e6n-${color} hover:bg-e6n-${color} hover:text-white` }><slot /></a>